/**
 * Created by Mathew on 10/22/2016.
 */

/**
 * Compute the Perlin Noise value at coordinates X, Y
 *
 * Noise is used to determine what map biome should be rendered at a point X, Y.
 * Using a seeded hashmap, 
 *
 * @param x
 * @param y
 * @returns {number}
 */
var noise = function (x, y) {
  var value = 0;

  return value;
}